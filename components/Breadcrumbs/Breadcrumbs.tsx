



interface Props {
    url_one?: string,
    text_one?: string
}

const Breadcrumbs: React.FC<Props> = ({url_one, text_one}) => {
    return (
        <>
        </>
    )
}
export default Breadcrumbs;